# T4_partial_state

Demonstrates crash mid-run + resume from a file-backed checkpoint.
The grader asserts the final artifact exists and that a resume occurred
(checkpoint next_step advanced; trace metadata resume=true).
